{
  "name": "@framebranch/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --exclude tests/fuzz.test.ts",
    "fuzz": "node tests/run-fuzz.mjs",
    "benchmark": "pnpm exec vite-node benchmarks/run.mjs",
    "coverage": "vitest run --exclude tests/fuzz.test.ts --coverage --coverage.include='src/**'"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^5.9.3",
    "vite-node": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
