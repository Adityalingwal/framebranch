import { ComingInM8b } from "./ComingInM8b";

/** §2 OUT: real body is M8b — the tab shell here is real, the body isn't. */
export function ChangesPanel() {
  return <ComingInM8b label="Changes" />;
}
